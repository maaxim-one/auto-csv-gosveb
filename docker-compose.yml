services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_DEBUG=0
      - SECRET_KEY=${SECRET_KEY:-}
    volumes:
      - storage:/app/storage
    restart: unless-stopped

volumes:
  storage:
