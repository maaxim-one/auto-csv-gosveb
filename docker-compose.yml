services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_DEBUG=0
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
    volumes:
      - temp_data:/app/temp_data
    restart: unless-stopped

volumes:
  temp_data:
